# GoReleaser config — builds prebuilt binaries and attaches them to a
# GitHub Release on every pushed tag (see .github/workflows/release.yml).
# Docs: https://goreleaser.com
version: 2

project_name: pickmem

before:
  hooks:
    - go mod tidy

builds:
  - id: pickmem
    main: ./cmd/pickmem
    binary: pickmem
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath   # strip local build paths -> reproducible, no path leakage
    # Reproducible builds: pin the binary's timestamp to the commit's.
    mod_timestamp: "{{ .CommitTimestamp }}"
    ldflags:
      - -s -w -X github.com/qwgao/pickmem/internal/cli.Version={{.Version}}
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64

# Print the size of each built binary in the CI logs.
report_sizes: true

archives:
  - id: pickmem
    # e.g. pickmem_0.1.0_darwin_arm64.tar.gz  (Windows -> .zip)
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - USER_GUIDE.md
      - LICENSE

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  groups:
    - title: Features
      regexp: '^.*?feat(\(.+\))??!?:.+$'
      order: 0
    - title: Bug fixes
      regexp: '^.*?fix(\(.+\))??!?:.+$'
      order: 1
    - title: Others
      order: 999
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  draft: false
  prerelease: auto
